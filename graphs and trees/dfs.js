function dfs(tree, target) {
  // add current node val to arr,
  // check if left exists, if so dfs on left with curr arr
  // if left is nul, dfs on right with curr arr
  // return curr arr once iterated through all nodes
}
